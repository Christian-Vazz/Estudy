// Tipo Null
let valorNull = null; // Quando a atribuição é intencional

// Tipo Undefined
let valorUndefined;
let Undefined = undefined; // Uma variável é undefined quando não possui nenhum valor (nem null) quando é declarada ou quando atribui undefined a ela (que dá no mesmo)